人:
    需求:
        文档
        需求串讲
    开发:
        其他:
            开发工具: idea maven- git CiCd 面板, bug 需求, 石墨, 钉钉, 视频会议
            框架: 缓存, mysql, redis,大数据-存储(es|mongo),消费(kafka),查询,中间件:kafka websocket等,性能-定时 线程池 缓存
            UI:
            仓库版本管理:
                代码库分支:
                    git flow:
                        feature 分支
                        dev分支
                        release分支
                        master分支
                        hotfix分支
        文档:
            使用:
            开发:
                流程图
                表结构
                背景
        接口(文档|权限|日志|缓存|接口文档|代码检查|):
            查询:
                条件
                name type等
                list单表查询--分布式
                一条数据异常,导致列表查询异常
            新增(修改|删除):
                createBy 等
                防重
                幂等
                一对多
                更新与新增合并-- ??
            统计:
                归类与统计:
                    布控多表统计,,
                    为了统计,增加标识,写入库复杂;
                    为了解决问题引入未知,
                    改变前提条件,(思考本质?还是未解决当前问题偷懒?)
                    -- 统计依赖标识  设备视频预警时间
        数据库表结构:
            主键|索引|注释|类型|长度|json字段-- 抽字段(极致扁平化)
            宽表设计
            逻辑删除
    test:
        bug追踪
    部署:
        监控
        镜像化 CICD
        上线升级:sql脚本
        数据库备份
    服务管理:
            微服务
            版本升级
            文件服务器 文件压缩